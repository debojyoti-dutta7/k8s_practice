apiVersion: v1
kind: Service
metadata:
  namespace: snapee
  name: extrahourcron-svc
spec:
  selector:
    app: extrahourcron
  ports:
    - protocol: TCP
      port: 5004
      targetPort: 8000
  type: ClusterIP
