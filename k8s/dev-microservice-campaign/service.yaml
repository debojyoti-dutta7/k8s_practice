apiVersion: v1
kind: Service
metadata:
  namespace: snapee
  name: dev-microservice-campaign-svc
spec:
  selector:
    app: dev-microservice-campaign
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
  type: ClusterIP
